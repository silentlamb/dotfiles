#!/bin/bash

EXPECTED_ARGS=1

if [ $# -ne $EXPECTED_ARGS ]
then
    echo "Usage: `basename $0` {branch}"
    exit $E_BADARGS
fi

branch_name=$1

git checkout -b mglinski/${branch_name}
if [ $? == 0 ];
then
    git push --set-upstream origin mglinski/${branch_name}
fi

